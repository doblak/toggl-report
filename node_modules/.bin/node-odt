#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//usr/bin/node" ]; then
  "$basedir//usr/bin/node"  "$basedir/../odt/bin/node-odt" "$@"
  ret=$?
else 
  /usr/bin/node  "$basedir/../odt/bin/node-odt" "$@"
  ret=$?
fi
exit $ret
